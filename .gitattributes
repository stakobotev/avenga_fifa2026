# Keep shell scripts LF so they run under Git Bash / on the Linux server,
# regardless of each developer's core.autocrlf setting.
*.sh text eol=lf

# Windows-only scripts stay CRLF.
*.ps1 text eol=crlf
